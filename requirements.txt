# Pin known working versions for Streamlit Cloud deploys
# 
# CRITICAL: opencv-python-headless MUST come first, before any package
# that might request opencv-python (GUI version).
# Streamlit Cloud uses headless environment - no display server available.

opencv-python-headless==4.11.0.86
numpy==1.26.4
Pillow==12.2.0
setuptools<82

# ML/Detection stack (depends on opencv-python-headless above)
ultralytics==8.4.49
easyocr==1.7.2
torch==2.12.0
torchvision==0.27.0

# Web framework
streamlit==1.57.0

# Utilities
reportlab==4.5.1
qrcode==8.2
